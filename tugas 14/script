// function calculateArea(length, width) {
//  return length * width;
// }

// TUGAS 1
let expression = function (length, width) {
    return length * width;
}
  console.log(expression(10, 5));

// TUGAS 2
let arrow = (length, width) => {
    return length * width;
}
  console.log(arrow(10, 5));